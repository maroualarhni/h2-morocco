# H2 Morocco — Configuration (copier en .env et adapter)
# Ne JAMAIS committer le fichier .env dans Git

# ── PostgreSQL ────────────────────────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=h2morocco_db
DB_USER=postgres
DB_PASSWORD=votre_mot_de_passe_ici

# Construit automatiquement depuis les variables ci-dessus :
# DB_URL=postgresql+psycopg2://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# ── Chemins (optionnel — valeurs par défaut dans le code) ─────────────────────
# OUTPUT_DIR=~/Downloads/H2Morocco222_Outputs
# CLEAN_CSV_DIR=~/Downloads/h2pipeline/outputs/clean_csv
